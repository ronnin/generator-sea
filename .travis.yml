language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-seajs' ]; then cd .. && eval "mv $currentfolder generator-seajs" && cd generator-seajs; fi

